import plotly.plotly as py
from plotly.graph_objs import *
py.sign_in("CarlosA_xxxxx", "apikey_xxx")
import numpy as np
x = np.random.randn(500)

data = Data([
    Histogram(
        x=x
    )
])
plot_url = py.plot(data, filename='basic-histogram')
